
package ch.hearc.meteo.imp.afficheur.real.moo;

import java.util.ArrayList;

import org.jfree.data.time.TimeSeries;
import org.jfree.data.time.TimeSeriesCollection;

import ch.hearc.meteo.spec.com.meteo.listener.event.MeteoEvent;

public class Manager
	{

	/*------------------------------------------------------------------*\
	|*							Constructeurs							*|
	\*------------------------------------------------------------------*/

	public Manager()
		{
		this.collectionAltitude = new TimeSeriesCollection();
		this.collectionPression = new TimeSeriesCollection();
		this.collectionTemperature = new TimeSeriesCollection();

		this.seriesAltitude = new ArrayList<TimeSeries>();
		this.seriesPression = new ArrayList<TimeSeries>();
		this.seriesTemperature = new ArrayList<TimeSeries>();
		}

	/*------------------------------------------------------------------*\
	|*							Methodes Public							*|
	\*------------------------------------------------------------------*/

	public void printAltitude(MeteoEvent event)
		{
		manage(seriesAltitude, event);
		}

	public void printPression(MeteoEvent event)
		{
		manage(seriesPression, event);
		}

	public void printTemperature(MeteoEvent event)
		{
		manage(seriesTemperature, event);
		}

	/*------------------------------*\
	|*				Set				*|
	\*------------------------------*/
	

	/*------------------------------*\
	|*				Get				*|
	\*------------------------------*/
	
	public TimeSeriesCollection getCollectionAltitude()
		{
		return this.collectionAltitude;
		}

	
	public TimeSeriesCollection getCollectionPression()
		{
		return this.collectionPression;
		}

	
	public TimeSeriesCollection getCollectionTemperature()
		{
		return this.collectionTemperature;
		}

	/*------------------------------------------------------------------*\
	|*							Methodes Private						*|
	\*------------------------------------------------------------------*/

	private void manage(ArrayList<TimeSeries> series, MeteoEvent event)
		{
		series.add(new TimeSeries("3"));
		System.out.println(series.get(0).getKey());
		}

	/*------------------------------------------------------------------*\
	|*							Attributs Private						*|
	\*------------------------------------------------------------------*/

	// Tools
	private TimeSeriesCollection collectionAltitude;
	private TimeSeriesCollection collectionPression;
	private TimeSeriesCollection collectionTemperature;
	private ArrayList<TimeSeries> seriesAltitude;
	private ArrayList<TimeSeries> seriesPression;
	private ArrayList<TimeSeries> seriesTemperature;

	}
